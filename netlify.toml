[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

# SPA Fallback Redirect for React Router & Vite
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
